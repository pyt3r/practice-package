name: test-env

channels:
  - conda-forge
  - defaults

dependencies:
  - conda-build
  - pip
  - pyyaml

  # == dev dependencies ==
  - coverage
  - flake8

  # == lib dependencies ==
  - python-graphviz
  - pytables
  - pandas
  - xlsxwriter
  - scipy
  - matplotlib
  - redis-py
  - requests