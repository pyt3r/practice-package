name: test-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.8.*
  - conda-build
  - pyyaml

  # == dev dependencies ==
  - coverage
  - flake8

  # == lib dependencies ==
  - python-graphviz
  - pytables
  - pandas
  - xlsxwriter
  - scipy
  - matplotlib
