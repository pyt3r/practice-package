name: rtd-env
channels:
  - conda-forge
  - defaults
dependencies:
  # build dependencies:
  - python=3.8
  - pip
  - pyyaml
  - sphinx
  - sphinx_rtd_theme
  - sphinx-gallery
  - jupyter_sphinx
  - pip:
      - rst2pdf
  # ./examples dependencies
  - pandas
  - matplotlib
  - scipy